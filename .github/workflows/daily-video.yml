name: AI ذكي متطور - يعمل كل ساعتين

on:
  schedule:
    # كل ساعتين - 6 مرات يومياً = 12 فيديو كل يوم!!!

    # 🌅 الساعة 6 صباحاً UTC (8 صباحاً بتوقيتك)
    - cron: '0 6 * * *'

    # ☀️ الساعة 8 صباحاً UTC (10 صباحاً)
    - cron: '0 8 * * *'

    # 🌞 الساعة 10 صباحاً UTC (12 ظهراً)
    - cron: '0 10 * * *'

    # ☀️ الساعة 12 ظهراً UTC (2 ظهراً)
    - cron: '0 12 * * *'

    # 🌤️ الساعة 2 ظهراً UTC (4 عصراً)
    - cron: '0 14 * * *'

    # 🌆 الساعة 6 مساءً UTC (8 مساءً)
    - cron: '0 18 * * *'

    # 🌃 الساعة 8 مساءً UTC (10 مساءً)
    - cron: '0 20 * * *'

    # 🌙 الساعة 10 مساءً UTC (منتصف الليل)
    - cron: '0 22 * * *'

  # تشغيل يدوي
  workflow_dispatch:
    inputs:
      focus_region:
        description: 'منطقة التركيز (فلسطين، سوريا، العالم الإسلامي)'
        required: false
        default: 'فلسطين والعالم الإسلامي'

jobs:
  generate-intelligent-video:
    runs-on: ubuntu-latest

    steps:
      - name: 🤖 نظام AI ذكي متطور
        run: |
          echo "═══════════════════════════════════════════"
          echo "🧠 AI ذكي يتعلم ويبحث ويحكي الحق"
          echo "⏰ الوقت: $(date)"
          echo "═══════════════════════════════════════════"

          curl -s -X POST 'https://backend.composio.dev/api/v1/cli/execute-recipe' \
          -H 'Content-Type: application/json' \
          -d '{
            "recipeId": "rcp_GA3Odr3U5NZ-",
            "input": {
              "report_email": "hamzawatfa25@gmail.com",
              "channel_handle": "@hamzawatfa-c3f",
              "focus_region": "${{ github.event.inputs.focus_region || 'فلسطين والعالم الإسلامي' }}"
            }
          }'

          echo ""
          echo "✅ تم تنفيذ النظام!"
          echo "📧 تفقد بريدك: hamzawatfa25@gmail.com"
          echo "═══════════════════════════════════════════"

      - name: 📝 تسجيل النتيجة
        if: always()
        run: |
          echo "📅 التاريخ: $(date)"
          echo "🎯 Recipe: rcp_GA3Odr3U5NZ-"
          echo "📺 القناة: @hamzawatfa-c3f"
          echo "🤖 AI ذكي متطور يتعلم من الأحداث"

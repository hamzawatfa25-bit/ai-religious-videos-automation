name: AI واعي - فيديوهات دينية متعددة المرات

on:
  schedule:
    # 🌅 3 صباحاً UTC (5 صباحاً مصر)
    - cron: '0 3 * * *'

    # 🌅 6 صباحاً UTC (8 صباحاً مصر)
    - cron: '0 6 * * *'

    # ☀️ 9 صباحاً UTC (11 صباحاً مصر)
    - cron: '0 9 * * *'

    # ☀️ 12 ظهراً UTC (2 ظهراً مصر)
    - cron: '0 12 * * *'

    # 🌆 3 عصراً UTC (5 عصراً مصر)
    - cron: '0 15 * * *'

    # 🌆 6 مساءً UTC (8 مساءً مصر)
    - cron: '0 18 * * *'

    # 🌙 9 مساءً UTC (11 مساءً مصر)
    - cron: '0 21 * * *'

    # 🌙 11 مساءً UTC (1 صباحاً مصر)
    - cron: '0 23 * * *'

  workflow_dispatch:
    inputs:
      video_topic:
        description: 'موضوع (فارغ = AI واعي يختار حسب الأحداث)'
        required: false
        default: ''

jobs:
  ai-aware-video-generation:
    runs-on: ubuntu-latest

    steps:
      - name: 🎬 نظام AI واعي ومتطور
        run: |
          echo "╔══════════════════════════════════════════╗"
          echo "║  🤖 AI واعي - يتعلم ويطور نفسه        ║"
          echo "║  ⏰ الوقت: $(date)                     ║"
          echo "╚══════════════════════════════════════════╝"
          echo ""
          echo "🔍 AI يبحث عن آخر الأخبار..."
          echo "🧠 AI يتعلم من الأحداث..."
          echo "✅ AI يقول الحق فقط..."
          echo "🤲 AI يدعو للمظلومين..."
          echo ""

          curl -s -X POST 'https://backend.composio.dev/api/v1/cli/execute-recipe'           -H 'Content-Type: application/json'           -d '{
            "recipeId": "rcp_GA3Odr3U5NZ-",
            "input": {
              "report_email": "hamzawatfa25@gmail.com",
              "channel_handle": "@hamzawatfa-c3f",
              "focus_on_current_events": "yes"
            }
          }'

          echo ""
          echo "✅ تم تنفيذ النظام بنجاح!"
          echo "📧 تفقد بريدك: hamzawatfa25@gmail.com"
          echo "🌍 محتوى واعي وصادق ومتطور!"
          echo "═══════════════════════════════════════════"

      - name: 📊 سجل التنفيذ
        if: always()
        run: |
          echo "📅 $(date)"
          echo "🎬 Recipe: rcp_GA3Odr3U5NZ-"
          echo "📺 القناة: @hamzawatfa-c3f"
          echo "🌍 AI واعي بالأحداث العالمية"
          echo "🤲 يدعو للمظلومين"
          echo "✅ يقول الحق فقط"
